generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/client"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Admin {
  id              Int      @id @default(autoincrement())
  name            String
  email           String   @unique
  password        String
  refreshToken    String?
  isEmailVerified Boolean  @default(false)  // ✅ Add this line
  createdAt       DateTime @default(now())
}



model Document {
  id               Int       @id @default(autoincrement())
  title            String
  summary          String
  content          String
  author           String
  publicationYear  Int
  sourceUrl        String?     // optional: link to where the article originated
  cloudinaryUrl    String      // for the uploaded file
  createdAt        DateTime    @default(now())
  tags             Tag[]       // many-to-many relation
}


model Tag {
  id        Int        @id @default(autoincrement())
  name      String     @unique
  documents Document[] // inverse side of many-to-many
}
